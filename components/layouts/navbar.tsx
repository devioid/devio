export const Navbar = ({ className }: { className?: string }) => {
  return (
    <div
      className={`${className} min-h-16 z-10 bg-gray-800 w-full flex flex-row`}
    >
      <p className="h-16">Nav</p>
    </div>
  )
}
